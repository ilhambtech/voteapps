pipeline {
    agent { label 'docker' }
        stages {
            stage('Run docker') {
            steps {
                container('docker'){
                    sh 'ls -lah ; docker build -t ilosaurus/vote .'   
                }
                
            }
        }
    }
}

