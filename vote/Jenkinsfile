pipeline {
    agent { label 'docker' }
        stages {
            stage('Run docker') {
            steps {
                container('docker'){
                    sh 'docker build -t ilosaurus/vote vote/Dockerfile'   
                }
                
            }
        }
    }
}

