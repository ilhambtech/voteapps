pipeline {
    agent { label 'docker' }
        stages {
            stage('Run docker') {
            steps {
                container('docker'){
                    sh 'cat /etc/os-release; docker version; ls -lah ; cd vote; docker build -t ilosaurus/vote .'   
                }
                
            }
        }
    }
}

