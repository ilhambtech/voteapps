pipeline {
    agent { label 'docker' }
        stages {
            stage('Run docker') {
            steps {
                container('docker-client'){
                    sh """
                        cat /etc/os-release;
                        ls -lah;
                        cd vote;
                        which docker;
                    """
                }
                
            }
        }
    }
}

