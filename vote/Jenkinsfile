pipeline {
  stages {
    stage('Run docker') {
      
      steps {
          sh """
              ls -lah; 
              cd vote;
              /kaniko/executor --context `pwd` --verbosity debug --destination ilosaurus/vote:latest
          """
        
          
      }
    }
  }
}
